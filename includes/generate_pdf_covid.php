// detail.php

// Incluez le fichier generate_pdf.php
require('generate_pdf.php');

// Données factices du patient (remplacez par les données réelles)
$nom = "Nom du patient";
$prenom = "Prénom du patient";
$score = 75;
$date = "2023-10-03";
$heure = "15:30:00";

// Appelez la fonction generatePDF pour générer le PDF
$pdf->generatePDF($nom, $prenom, $score, $date, $heure);

// Étape 8 : Générez le fichier PDF et affichez-le dans le navigateur
$pdf->Output();
// detail.php

// Incluez le fichier generate_pdf.php
require('generate_pdf.php');

// Données factices du patient (remplacez par les données réelles)
$nom = "Nom du patient";
$prenom = "Prénom du patient";
$score = 75;
$date = "2023-10-03";
$heure = "15:30:00";

// Appelez la fonction generatePDF pour générer le PDF
$pdf->generatePDF($nom, $prenom, $score, $date, $heure);

// Étape 8 : Générez le fichier PDF et affichez-le dans le navigateur
$pdf->Output();
// detail.php

// Incluez le fichier generate_pdf.php
require('generate_pdf.php');

// Données factices du patient (remplacez par les données réelles)
$nom = "Nom du patient";
$prenom = "Prénom du patient";
$score = 75;
$date = "2023-10-03";
$heure = "15:30:00";

// Appelez la fonction generatePDF pour générer le PDF
$pdf->generatePDF($nom, $prenom, $score, $date, $heure);

// Étape 8 : Générez le fichier PDF et affichez-le dans le navigateur
$pdf->Output();
